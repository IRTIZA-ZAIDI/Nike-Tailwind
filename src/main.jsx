import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx'; // Update the path to your App component
import './index.css'; // Update the path to your index.css file

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
